@{
    ViewData["Title"] = "Check Database Connection";
}

<div class="container text-center mt-5">
    <h1>Check Database Connection</h1>
    <p>This endpoint returns HTTP 200 if the database connection is established, or HTTP 400 if unreachable.</p>
    <a href="@Url.Action("Index", "Home")" class="btn btn-secondary mt-3">← Back to Home</a>
</div>
